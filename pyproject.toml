[project]
name = "media-organiser"
version = "0.1.0"
description = "A local-first media management application."
requires-python = ">=3.12"

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
